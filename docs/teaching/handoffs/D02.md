# KIWI Teaching — D02 → D03 Implementation Handoff

**Delivery:** D02 — Authoritative Runtime, Event & Validation Primitives  
**State:** ACCEPTED  
**Date:** 2026-09-25  
**Canonical roadmap version:** Phase 19 v1.4 FINAL  
**Start commit:** `bf593c10087f81a2fbcc08cc145baa2f40a33c72`  
**Accepted implementation merge:** `eba01147b4be1bb5293ddc15b6d6f4cc8282c469`  
**Implementation PR:** #85  
**Pre-merge tested head:** `c0c320282e577f5d6947447cf0ec42cbfa67174c`

## 1. Scope

**Planned TCH IDs:**

- TCH-0738–TCH-0743
- TCH-0745–TCH-0747
- TCH-0753–TCH-0758

**Completed TCH IDs:**

- TCH-0738 — durable server-authoritative due-event execution foundation
- TCH-0739 — browser timer/countdown projection from authoritative server timestamps
- TCH-0740 — durable replay/reconciliation across restart, duplicate, stale and late delivery
- TCH-0741 — runtime/restart/idempotency/fairness test coverage
- TCH-0742 — centralized KIWI AI Orchestrator-only Teaching execution boundary
- TCH-0743 — responsibility-class separation; deterministic T0 is not mapped to a model call
- TCH-0745 — safe runtime/AI execution observability and correlation metadata
- TCH-0746 — initial D02 Teaching threat model
- TCH-0747 — reusable Teaching accessibility baseline
- TCH-0753 — trusted schema/domain validation before model output can proceed
- TCH-0754 — no raw/freeform/schema-only model output can directly mutate academic truth
- TCH-0755 — T0–T4 authority-aware fail-closed behavior
- TCH-0756 — untrusted-content isolation and context-minimization primitives
- TCH-0757 — deterministic-authority precedence over conflicting model output
- TCH-0758 — intelligence-class/authority/validation/rejection/owner-mutation telemetry without chain-of-thought persistence

**Deferred/not completed:** NONE.

D02 deliberately did not implement D03 Capability Registry/prompt-runtime behavior, D04 Teaching academic persistence, or later Course/Scheduler/Assessment/Gradebook/SKM/Attendance/Progression behavior.

## 2. Canonical sources actually used

Materially relied upon:

- `KIWI_Teaching_Implementation_Context_Pack_v1.7.md`
- `KIWI_Teaching_Phase22_Final_Implementation_Freeze_v1.3.md`
- `KIWI_Teaching_Phase22_Manifest_v1.3.json`
- `KIWI_Teaching_System_Blueprint-11.5.md` / canonical Blueprint v11.5 semantics
- `KIWI_Teaching_Master_Implementation_Backlog-9.5.md`
- `KIWI_Teaching_Delivery_Task_Map_v1.5.json`
- `KIWI_Teaching_Phase19_Delivery_Roadmap_Overlay_v1.4_FINAL.md`
- `KIWI_Teaching_Capability_Registry_v1.1.md`
- `KIWI_Teaching_Orchestrator_Event_Runtime_Spec_Phases_7-8_v1.1.md`
- `KIWI_Teaching_Progressive_Preparation_Lifecycle_Standard_v1.0.md`
- `KIWI_Teaching_Critical_Invariant_Trace_Map_v1.1.md`
- `KIWI_Teaching_Implementation_Change_Control_Protocol_v1.0.md`
- accepted `docs/teaching/handoffs/D01.md`
- current live GitHub `main`, Supabase schema/migrations, Vercel deployment state, and current KIWI AI Orchestrator implementation

### Authority-source integrity resolution

The accepted D01 handoff warned that a retained copy named as the Phase 4–6 Authority Spec did not match the frozen v1.1 SHA-256. A later user-supplied v1.2 copy contained the post-PPL 169-capability / 22-T0 authority model.

D02 did **not** silently treat that v1.2 file as a new frozen architecture source. It used it only where its authority semantics were independently confirmed by the frozen Final Freeze, Context Pack, Blueprint v11.5, Capability Registry, runtime spec and D00 governance. No D02 implementation depends on a rule unique to an unverified superseding document.

## 3. Implementation changes

### Durable event/runtime foundation

Added:

- `teaching/runtime/constants.js`
- `teaching/runtime/time-projection.js`
- `teaching/runtime/reconciliation.js`
- `teaching/runtime/postgres-event-store.js`
- `teaching/runtime/durable-event-runtime.js`
- `teaching/runtime/index.js`

Key behavior:

- due events are persisted before execution;
- browser tabs are never authoritative time owners;
- server timestamps are the source for countdown/timer projections;
- durable claims use bounded leases and `FOR UPDATE SKIP LOCKED`;
- idempotency keys prevent duplicate authoritative execution;
- expired claims become recoverable retry work;
- every academic event handler must reconcile current authoritative state before acting;
- reconciliation distinguishes actionable, already-satisfied, superseded and fairness-recovery-required states;
- exhausted KIWI-side execution failures enter explicit fairness recovery instead of fabricating a student penalty;
- no D02 runtime object owns Course scope, Scheduler truth, Gradebook, SKM, Assessment, Attendance, Request or Progression truth.

### Event contracts

Changed:

- `teaching/events/contracts.js`
- `teaching/events/names.js`

D02 adds distinct authoritative due-event vocabulary for:

- Class start due;
- break end/expiry due;
- assessment expiry due;
- Class end due;
- approved Request effective time due.

Event envelopes now distinguish commands, committed domain events, scheduled due events, intelligence results and operational/recovery events. Authenticated input cannot masquerade as an authoritative committed/system-time event.

Event payloads are bounded routing metadata rather than an alternate source-content or academic-state store.

### Central AI execution and validation

Added:

- `teaching/ai/contracts.js`
- `teaching/ai/failure-policy.js`
- `teaching/ai/output-validation.js`
- `teaching/ai/central-orchestrator-boundary.js`
- `teaching/authority/owners.js`
- `teaching/authority/deterministic-precedence.js`
- `teaching/authority/commit-gateway.js`

Invariants:

- Teaching feature/domain code does not call a provider SDK directly;
- model work enters through the injected central KIWI AI Orchestrator runner only;
- T0 responsibilities are rejected from the model path;
- T2–T4 require schema and domain validation before a result can receive the trusted internal validation brand;
- deterministic-authority checks may veto schema-valid/model-plausible output;
- the authoritative commit gateway accepts only branded validated results;
- the gateway can only hand accepted output to a registered authoritative owner service;
- the gateway itself owns no academic truth.

D02 does not create the D03 Capability Registry runtime, prompt-family dispatcher, Constitution binder or Teaching AI route manifest.

### Failure behavior

Authority-aware failure policy implemented:

- T0 — deterministic state does not depend on AI;
- T1 — safe constrained communication fallback may be used;
- T2 — invalid/failed interpretation cannot mutate durable evidence;
- T3 — invalid/failed planning/generation remains draft/pending;
- T4 — invalid/failed judgment cannot finalize an official mark.

All KIWI-caused failure paths are non-punitive.

### Context security

Added `teaching/security/context-lanes.js`.

It structurally separates:

- trusted authoritative state;
- permission constraints;
- provenance-linked academic content;
- explicitly untrusted Subject/upload/student/source/retrieved content.

Formal-marking context rejects irrelevant attendance, Teacher Personality, reputation, prior GPA, unrelated marks and behavioral history unless an approved academic policy explicitly requires an exception.

### Observability

Added `teaching/observability/postgres-execution-telemetry.js`.

Safe telemetry includes execution/event correlation, responsibility key, intelligence class, authority ceiling, central task/model identifier, contract/schema versions, validation outcome, rejection/safe-failure reason and eventual authoritative-owner mutation reference.

It does not persist hidden chain-of-thought, prompt bodies, student response bodies, assessment answers, source passages, secrets or tokens.

### Accessibility

Added:

- `teaching/accessibility/baseline.js`
- `docs/teaching/accessibility/d02-baseline.md`

Minimum reusable primitive contract:

- keyboard operability;
- semantic structure;
- visible focus;
- status not encoded by color alone;
- text-scaling/reflow safety.

### Threat model

Added `docs/teaching/security/d02-threat-model.md`.

Threats addressed at the D02 layer include grade/academic-record tampering paths, assessment leakage boundaries, RLS/Data API bypass, privilege escalation, prompt/content injection, stale/duplicate event execution and invalid/outage model behavior.

### Backend composition

`index.js` now composes the D02 Teaching runtime with the existing server-side PostgreSQL connection and the central KIWI AI Orchestrator.

Runtime initialization is fail-closed: if the D02 operational schema is unavailable, the Teaching due-event worker stays stopped. The backend does not substitute browser time or an in-memory academic-time authority.

### Verification/CI

Added:

- `scripts/verify-teaching-d02-runtime.js`
- `.github/workflows/teaching-d02-runtime.yml`
- `tests/teaching/unit/d02-runtime.test.js`
- `tests/teaching/integration/d02-runtime-schema.test.js`

D01 verification remains part of the D02 CI gate.

## 4. Persistence and migrations

Added migration:

`migrations/20260925_teaching_d02_runtime_primitives.sql`

Production Supabase recorded migration:

`20260925142813_teaching_d02_runtime_primitives`

New private schema:

`teaching_runtime`

New operational tables:

- `teaching_runtime.due_events`
- `teaching_runtime.event_attempts`
- `teaching_runtime.ai_execution_audit`

These tables are runtime coordination/audit persistence only. They are **not** D04 academic-domain persistence.

Security state verified after apply:

- RLS enabled on all three D02 tables;
- `anon` has no table grants;
- `authenticated` has no table grants;
- `service_role` has the intended server-side CRUD grants;
- the existing `public.background_jobs` table is not used for Teaching academic due-event authority.

A duplicate migration-history record was briefly created by an idempotent re-apply after the migration had already been applied. The duplicate history row `20260925145842` was removed using migration-history repair semantics; the original `20260925142813` record remains. No duplicate table/schema object was created and the D02 schema was not rolled back.

Rollback/recovery posture:

- stop the Teaching D02 worker first;
- because the schema stores operational coordination/audit state rather than academic truth, emergency rollback is to remove/disable the runtime integration and, only if intentionally abandoning D02 state, drop `teaching_runtime`;
- normal forward recovery should prefer fixing the worker/schema rather than destroying event/audit history.

Production data migration: NONE.

### Existing Supabase findings not owned by D02

Supabase advisors continue to report five pre-existing public tables with RLS disabled:

- `public.background_jobs`
- `public.biome_zones`
- `public.biome_zone_descriptions`
- `public.onboarding_state`
- `public.notifications`

D02 did not blindly enable RLS because correct application policies/compatibility are not defined by this delivery.

The advisor also reports `RLS enabled, no policy` on the private D02 tables. This is expected for D02: browser roles have no schema/table grants and the intended access path is trusted server-side/direct PostgreSQL or service role. No browser policy is required for these private operational tables.

## 5. Interfaces and invariants

New/changed interfaces the next delivery must preserve:

- `createTeachingRuntimePlatform()` — operational composition only; not an academic owner.
- `createPostgresTeachingEventStore()` — durable scheduled-event persistence/claiming.
- `createDurableTeachingEventRuntime()` — event lease/retry/reconcile/handler execution.
- `projectAuthoritativeTimer()` — browser/UI projection only; never academic authority.
- `createCentralAIExecutionBoundary()` — only Teaching model-execution seam.
- `validateModelOutput()` — trusted schema/domain/deterministic validation gate.
- `createDeterministicAuthorityCheck()` — deterministic truth precedence.
- `createAuthoritativeCommitGateway()` — owner-service handoff only after trusted validation.
- `buildSeparatedContextLanes()` / `asUntrustedData()` — trusted/untrusted structure.
- `createPostgresTeachingExecutionTelemetry()` — safe metadata/provenance logging.
- D02 accessibility primitive baseline.

Non-negotiable carry-forward invariants:

- browser time/UI state are not authoritative;
- AI output is never direct academic truth;
- T0 deterministic authority cannot be overridden by model output;
- event runtime never calls a provider directly;
- Teaching model work routes through central KIWI AI orchestration;
- stale/late/duplicate events reconcile against current authoritative state;
- long-running/intelligence results cannot overwrite newer authoritative state;
- KIWI-caused failure cannot directly create student academic penalty;
- Gradebook truth, SKM truth and Progression truth remain separate;
- no D03 prompt/capability binding may raise a capability above its canonical authority ceiling;
- no generic Teaching feature-availability toggles may be reintroduced.

## 6. Tests actually executed

Merged D02 commit `eba01147b4be1bb5293ddc15b6d6f4cc8282c469` has green GitHub checks:

- `verify-d02` — SUCCESS
- `verify-d01` — SUCCESS
- `ai-tests` — SUCCESS

D02 CI evidence:

- `npm run verify:teaching:d01` — PASS
- `npm run verify:teaching:d02` — PASS
- `node --check index.js` — PASS
- syntax check of every `teaching/**/*.js` module — PASS
- `npm run test:teaching` — **32 tests, 32 passed, 0 failed, 0 skipped**
- `npm test` — **394 tests, 394 passed, 0 failed, 0 skipped**
- `npm run build:web` — PASS
- `npm run test:teaching:integration` — **4 tests, 2 passed, 0 failed, 2 skipped**

Integration skips are deliberate because no non-production Supabase test project/secrets are configured:

- D02 live non-production private-schema smoke test — skipped;
- D01 live non-production foundation-table smoke test — skipped.

Both production-refusal guards passed; integration tests never fall back to production.

A separate validation-only PR #86 was opened after merge solely to retrigger the D02 workflow against the merged tree. Its D02 CI job also completed successfully. PR #86 was closed unmerged and changes no production code.

## 7. Security / privacy / academic-authority review

Passed/implemented:

- browser roles cannot access the private D02 runtime tables;
- RLS enabled on D02 operational tables;
- no provider SDK imports in Teaching feature/domain code;
- no raw model-output authoritative write path;
- deterministic-authority conflict rejection;
- server-only privileged mutation boundary preserved;
- untrusted Subject/upload/student/source content remains data, not control instructions;
- formal-marking context-minimization guard;
- safe telemetry excludes chain-of-thought and raw sensitive academic content;
- retries/replay are idempotent and state-reconciled;
- exhausted KIWI-side runtime failure enters fairness recovery, not student penalty.

Historical Git history may still contain a previously exposed database credential identified during D01. Active source no longer contains the fallback. Credential rotation remains a broader operational security follow-up.

## 8. Known defects and debt

No known P0/P1 defect attributable to D02 remains unresolved.

Carry-forward items:

1. **Non-production Supabase branch/test project not configured.**  
   Severity: test-infrastructure debt.  
   Production-refusal guards are active; live integration smoke tests remain skipped until a safe non-production database is supplied.

2. **Five pre-existing public RLS-disabled tables remain outside D02 ownership.**  
   Severity: existing platform security debt.  
   D02 does not depend on them for authoritative Teaching runtime state.

3. **Historical credential rotation follow-up remains from D01.**  
   Active code is clean; repository history may still contain the old secret.

4. **Render deployment not independently re-queried in this execution session.**  
   The Render connector requires explicit user confirmation of the workspace before service queries and this session did not receive that confirmation. No Render environment variable/configuration change was required by D02. D03 preflight should re-check the current Render backend deployment/logs before any live runtime-dependent D03 acceptance.

## 9. Architecture exceptions

NONE.

No frozen academic authority boundary was changed.

No prompt family was modified.

No provider/model ID was hard-coded into Teaching domain/feature code.

No Capability Registry/prompt runtime was pulled forward from D03.

No D04 academic persistence was pulled forward.

No duplicate Gradebook, SKM, Progression, Attendance, Course-scope, Assessment-Attempt, Request, Teacher-Identity or Scheduler truth was introduced.

## 10. Deployment/runtime impact

### GitHub

Implementation PR #85 merged to `main` as:

`eba01147b4be1bb5293ddc15b6d6f4cc8282c469`

GitHub checks on that SHA are green.

### Supabase

Production project:

`nqdwifqskxkblgdgeutn`

D02 migration applied and verified:

`20260925142813_teaching_d02_runtime_primitives`

No academic-domain data migration was required.

### Vercel

Production deployment for the D02 merged SHA is READY:

- deployment: `dpl_CHoJ3xhBmEW23sga1AunGNHQVJGV`
- Git SHA: `eba01147b4be1bb5293ddc15b6d6f4cc8282c469`
- target: production
- state: READY

### Render

No D02 Render environment/config change was required.

Current Render deployment was not independently queried in this execution because the connector requires an explicitly user-confirmed workspace selection. The accepted D01 handoff recorded the KIWI Render backend as LIVE; D03 must re-verify the current backend deployment/logs as part of its normal live-infrastructure preflight.

### New environment variables

No new required secret or product-availability environment variable was introduced.

Optional operational tunables:

- `TEACHING_RUNTIME_POLL_MS`
- `TEACHING_RUNTIME_LEASE_MS`
- `TEACHING_RUNTIME_BATCH_SIZE`
- `TEACHING_RUNTIME_MAX_ATTEMPTS`
- `TEACHING_RUNTIME_RETRY_BASE_MS`

Defaults are bounded in server code. These are runtime tuning values, not feature-availability flags.

Rollback trigger:

- D02 runtime schema readiness failure;
- repeated lease/claim corruption;
- evidence of duplicate authoritative owner mutation despite reconciliation/idempotency;
- privilege exposure of `teaching_runtime` to browser roles.

The runtime fails closed if schema readiness is absent.

## 11. Next-delivery readiness

**Next delivery:** D03 — Capability Registry, Prompt Runtime & Route-Control Foundation

**Prerequisites satisfied:** YES — D03 is unlocked. The prompt-artifact authority check in Section 15 is a mandatory D03 preflight and must be resolved before TCH-0856 can be accepted.

Evidence:

- D01 accepted;
- all 15 D02 TCH IDs completed;
- merged implementation SHA has green D01/D02/AI checks;
- private durable operational schema is applied and verified;
- server-time/event/idempotency/reconciliation primitives exist;
- central AI-only execution seam exists;
- trusted model-output validation and owner-commit boundary exist;
- baseline security, observability and accessibility contracts exist;
- no D03 behavior was implemented prematurely.

**Required carry-forward context:**

- use current `main`;
- preserve D02 runtime/authority interfaces listed above;
- do not bypass the central KIWI AI Orchestrator;
- capability/prompt contracts in D03 must declare and respect canonical T0–T4 ceilings;
- D03 must not convert prompt-family membership into academic authority;
- D03 must use D02 context lanes/validation/commit boundaries rather than creating parallel ones;
- Teaching AI routes remain UNQUALIFIED until D30;
- do not reintroduce generic Teaching feature flags;
- re-verify current Render deployment/logs at D03 preflight;
- no D03 work may silently resolve the broader five-table RLS debt unless its owning scope/change control explicitly requires it.

**Recommended minimum implementation baseline:**  
`eba01147b4be1bb5293ddc15b6d6f4cc8282c469`

D03 must still fetch current live `main` before coding. A documentation-only D02 handoff commit may exist after the minimum implementation baseline and should be included by starting from current `main`.

## 12. D02 acceptance record

- Delivery: D02
- State: ACCEPTED
- Implementation PR: #85
- Start SHA: `bf593c10087f81a2fbcc08cc145baa2f40a33c72`
- Pre-merge tested head: `c0c320282e577f5d6947447cf0ec42cbfa67174c`
- Accepted implementation merge: `eba01147b4be1bb5293ddc15b6d6f4cc8282c469`
- GitHub D02 CI: PASS
- GitHub D01 regression: PASS
- GitHub AI regression: PASS
- Teaching unit tests: 32/32
- full regression tests: 394/394
- web build: PASS
- Supabase production migration: APPLIED and verified
- Vercel production: READY
- architecture exceptions: NONE
- D03: UNLOCKED

After this handoff, stop. Do not begin D03 in this delivery.
---

## 13. D03 assignment and exact delivery boundary

**Delivery:** D03 — Capability Registry, Prompt Runtime & Route-Control Foundation  
**Milestone:** Foundation  
**Hard predecessor:** D02  
**Canonical task count:** 36  
**Canonical task-label mix:** AI 7, CORE 14, GATE 5, OPS 2, QA 6, SEC 2

**Exact D03 backlog scope:**

- TCH-0788–TCH-0790
- TCH-0792–TCH-0797
- TCH-0799–TCH-0816
- TCH-0818
- TCH-0833
- TCH-0856
- TCH-0859
- TCH-0872–TCH-0874
- TCH-0886–TCH-0887

D03 must implement **all and only** the canonical D03 scope unless a formally accepted change-control record explicitly changes the delivery assignment.

D03 must not begin D04 persistence work merely because a registry, prompt runtime, route manifest, preparation profile or Constitution binding would be easier to store in a new academic schema. D04 owns the kernel Teaching persistence/security/auditability delivery.

D03 must not implement the D05 Teaching Orchestrator service simply because D03 defines contracts that D05 will later consume. D03 may define/version contracts and adapters required by its own scope, but it must not pull forward D05 orchestration ownership, trigger normalization, authoritative domain mutation routing or T0 orchestration runtime behavior.

D03 must not mark any Teaching model route production-qualified. Empirical primary/fallback/stability/C4 qualification remains a D30 responsibility.

---

## 14. D03 canonical objective, acceptance gate and governing counts

The canonical D03 objective is to:

- implement the versioned Capability Registry runtime representation;
- register the canonical Teaching prompt-family baseline without silently rewriting prompt text;
- implement prompt-family/task-mode runtime contracts;
- bind the Teaching Constitution as an explicit versioned cross-family rule layer;
- structurally enforce authority/context/output/failure contracts rather than relying on prompt prose alone;
- define the Teaching AI route manifest outside prompt text;
- keep every route qualification state explicitly **UNQUALIFIED** until D30 evidence exists;
- add Preparation Profile and PPL route-posture contracts without hard-coding provider/model IDs;
- enforce the Phase-16 production hold.

### Current frozen acceptance counts

The final D03 acceptance baseline is:

- **169 total capabilities**;
- **147 model-eligible capabilities**;
- **22 T0 / no-direct-prompt capabilities**;
- **19 canonical prompt families**.

The four PPL T0 capabilities added after the original 165-capability inventory are:

- `teaching.preparation.workspace_state_transition`
- `teaching.preparation.materiality_staleness_reconciliation`
- `teaching.preparation.finalization_readiness_gate`
- `teaching.preparation.protected_content_isolation`

### Mandatory legacy-count resolution

Some older permanent D03 task descriptions still contain the historical **165 capabilities / 18 T0** wording. In particular, older text under TCH-0792 and TCH-0794 reflects the pre-PPL inventory.

That historical wording must **not** become the final D03 implementation count.

Within the same frozen delivery, TCH-0872 and the Phase-22/Context-Pack baseline supersede the final count semantics to **169 = 147 model-eligible + 22 T0**. The original 165 inventory identities remain traceable, and the original 18 T0 responsibilities remain valid, but the four new PPL T0 capabilities must also be present and promptless.

D03 registry tests must therefore close against the current frozen 169/147/22/19 baseline, not the historical 165/147/18/19 baseline.

---

## 15. Prompt-artifact authority and source-integrity gate

D03 is the first delivery that imports/registers the frozen prompt-family implementation baseline, so source authority must be resolved before TCH-0856 is marked complete.

At D02 handoff time, the accepted Phase-22 Final Freeze v1.3 and Implementation Context Pack v1.7 identify:

- `KIWI_Teaching_Phase15_Final_Prompt_Manifest_v1.2.json` as the current frozen prompt manifest;
- `KIWI_Teaching_Phase15_All_19_Prompt_Families_FINAL_DESIGN_FROZEN_v1.2.md` as the frozen combined prompt pack;
- all 19 prompt families as unchanged by the PPL freeze represented in those canonical Phase-22 artifacts.

A later supplied artifact named `KIWI_Teaching_Phase15_Final_Prompt_Manifest_v1.3.json` and its v1.3 combined prompt pack are also present in the implementation materials. That v1.3 manifest explicitly describes itself as a **post-freeze PPL prompt amendment** with D30 requalification pending and changes several family versions.

Therefore D03 must not silently choose whichever file has the larger version number.

Before importing prompt text/hashes for TCH-0856, the D03 implementation agent must:

1. inspect the current canonical Phase-22 freeze/manifest and Context Pack;
2. inspect any accepted post-freeze change-control artifacts that adopt the v1.3 prompt amendment;
3. verify whether the later v1.3 manifest/pack has been formally promoted into the canonical implementation baseline;
4. if it has been formally adopted, record the adopting change-control artifact, exact manifest/pack versions and hashes and keep all affected routes UNQUALIFIED pending D30;
5. if it has **not** been formally adopted, retain the Phase-22-authorized v1.2 runtime baseline and treat the v1.3 files as non-canonical pending change control;
6. never merge prompt bodies, cherry-pick individual newer family text or create a hybrid manifest by convenience;
7. if authority remains unresolved, block only the affected prompt-registration acceptance surface and record the issue explicitly rather than guessing.

The same rule applies to the previously identified Phase 4–6 Authority Spec source-integrity mismatch: an unverified retained copy must not be elevated merely because its filename appears newer.


---

## 16. Mandatory D03 preflight before any implementation

D03 must begin from the repository and canonical artifacts, not from conversational memory.

Before modifying code, the D03 implementation agent must:

1. Fetch the current live `main` SHA and record it as the D03 start baseline.
2. Read this accepted D02 → D03 handoff completely.
3. Read `KIWI_Teaching_Implementation_Context_Pack_v1.7.md`.
4. Read the D03 row in `KIWI_Teaching_Phase19_Delivery_Roadmap_Overlay_v1.4_FINAL.md`.
5. Resolve the exact 36 D03 TCH IDs in `KIWI_Teaching_Delivery_Task_Map_v1.5.json`.
6. Read every D03 task description in the canonical Backlog/Task Map and resolve conflicts using later frozen amendments/change control rather than numeric/task-text age alone.
7. Read the current Capability Registry and verify the governing **169 / 147 / 22 / 19** baseline.
8. Read the canonical Phase-15 prompt manifest and combined prompt-family pack only after resolving the source-authority issue in Section 15.
9. Read the Teaching Constitution / governing Blueprint sections required by TCH-0801–TCH-0816.
10. Read the PPL Standard and the PPL route/Preparation Profile requirements for TCH-0872–TCH-0874 and TCH-0886–TCH-0887.
11. Inspect the actual D02 runtime implementation under:
    - `teaching/ai/`
    - `teaching/authority/`
    - `teaching/security/`
    - `teaching/observability/`
    - `teaching/runtime/`
    - `teaching/events/`
12. Inspect the existing central KIWI AI Orchestrator seam that D02 injects into `createCentralAIExecutionBoundary()`.
13. Inspect the current migration history and confirm D03 does not accidentally create D04-owned academic persistence.
14. Inspect current Vercel deployment state.
15. Re-verify current Render backend/deployment/logs once the Render workspace is explicitly confirmed for the connector.
16. Inspect current Supabase schema/advisors for compatibility, but do not alter unrelated RLS debt merely to make D03 “clean.”
17. Record all pre-existing defects, skipped integration tests, source-integrity issues and unresolved architecture exceptions before implementation begins.
18. Create a D03 implementation branch/PR from current `main`; do not implement against a stale D02 implementation SHA when documentation or corrections exist later on `main`.

If current `main` conflicts materially with this accepted handoff, D03 must stop the affected implementation surface, document the mismatch and resolve it through repository evidence/change control. It must not silently choose the conversational version.

---

## 17. D03 implementation workstreams

D03 should be implemented as a set of explicit, testable workstreams rather than one monolithic “prompt system.”

### 17.1 Capability Registry runtime

Implement a versioned runtime representation of the canonical Capability Registry that exposes, at minimum:

- canonical capability ID;
- display name/description;
- execution class;
- T0–T4 authority ceiling;
- authoritative owner/boundary;
- model posture;
- prompt-family membership where model-backed;
- primary family identity;
- contract/schema refs;
- context requirements/prohibitions;
- commit posture;
- uncertainty/failure posture;
- lifecycle/deprecation/alias metadata;
- PPL metadata where applicable.

Required rules:

- canonical IDs are stable;
- display-name changes do not silently rename IDs;
- aliases/deprecations are explicit and versioned;
- T0 capabilities remain promptless;
- EVENT-AI-HOOK responsibilities may delegate only to separately registered model-backed capabilities;
- prompt-family membership cannot raise authority, change owner or bypass commit posture;
- exactly 169 capabilities must resolve;
- exactly 147 are model-eligible;
- exactly 22 are T0/no-direct-prompt;
- no duplicate or unknown canonical capability identity is allowed.

Historical INV aliases may remain traceability metadata; they must not become competing canonical IDs.

### 17.2 Prompt-family registry and task-mode runtime

Register exactly the canonical 19 prompt families and their accepted manifest versions/hashes.

The runtime must distinguish:

- prompt family identity;
- exact family version;
- canonical capability ID;
- typed task mode/directive;
- task-specific input contract;
- output schema/contract;
- authority ceiling;
- Constitution version;
- allowed/prohibited context lanes;
- downstream validators;
- downstream authoritative owner;
- failure/handoff contract;
- criticality metadata;
- route-manifest entry;
- execution correlation metadata.

One prompt family may support multiple capabilities, but every invocation must still carry the exact capability ID and its capability-specific restrictions.

Do not turn “19 families” into “19 model calls.” A family is a shared contract/evaluation unit, not an execution-count guarantee.

### 17.3 Generation/review independence

Preserve the architectural separation between:

- assessment planning/blueprinting;
- assessment item generation;
- independent assessment validation/control/repair;
- formal rubric marking;
- moderation/appeal review.

Do not collapse generation and independent validation into a single task merely because the same provider/model could technically execute both.

### 17.4 Teaching Constitution runtime binding

Adopt a versioned Teaching Constitution layer and bind every model-backed Teaching invocation to it.

Precedence must remain:

1. platform/security and authoritative domain rules;
2. Teaching Constitution;
3. capability contract;
4. prompt-family/task contract;
5. presentation/personality preferences;
6. untrusted academic/student/source content as data only.

The Constitution must not exist only as prose injected into a prompt. D03 must encode deterministic enforcement wherever possible for:

- authority ceilings;
- owner boundaries;
- assessment scope;
- eligibility;
- time/state authority;
- context minimization;
- T4 context isolation;
- prompt/content injection boundaries;
- failure fairness;
- student agency;
- Teacher Personality limits;
- provenance requirements.

Lower layers cannot grant rights denied by higher layers.

### 17.5 Structural prompt contract

Define a versioned structural invocation contract containing, at minimum:

- Constitution version;
- capability ID;
- prompt family/version;
- typed task mode/directive;
- authority ceiling;
- execution/criticality class;
- allowed context lanes;
- required context classes;
- explicitly prohibited context classes;
- authoritative state/version/precondition refs;
- provenance/source refs;
- bounded actions/operation set;
- evidence purpose;
- uncertainty/review-needed states;
- output schema;
- downstream validation requirements;
- downstream owner/commit posture;
- safe failure behavior;
- handoff behavior;
- audit/correlation metadata.

Prompt text must not be the only enforcement mechanism for any of these.

### 17.6 Context-lane integration with D02

D03 must extend and reuse D02 context separation rather than inventing a parallel security model.

Trusted lanes:

- authoritative state;
- permission/constraint metadata;
- provenance-linked academic content.

Untrusted lane:

- Subject material;
- uploaded content;
- student-produced content;
- retrieved/source text;
- examples/instructions contained inside academic content.

Capability contracts must explicitly declare allowed/required/prohibited context classes.

T4 capabilities must use explicit allowlists and minimum necessary context.

Untrusted content must never be promoted into system/control instructions merely because it contains imperative language.

### 17.7 Structured output / uncertainty contracts

Every model-backed capability must have a structured output contract that supports uncertainty.

Where applicable, results must be able to return states such as:

- insufficient evidence;
- unresolved conflict;
- review required;
- cannot determine safely;
- incomplete;
- rejected by deterministic authority;
- invalid due to stale state/version.

The runtime must not force confident classifications or generation where evidence is inadequate.

Student-facing prose and state-bearing classification/judgment fields must be separable where both exist.

### 17.8 Validation integration with D02

D03 must plug its richer contracts into the existing D02 validation boundary.

Every model result with durable consequence must be checked against:

- schema;
- capability identity;
- capability authority ceiling;
- Constitution version/rules;
- provenance requirements;
- domain constraints;
- authoritative state/version/preconditions;
- deterministic authority;
- downstream owner/commit posture.

A successful model call is not a successful academic operation.

### 17.9 T1–T4 fail behavior

D03 contract/runtime behavior must preserve:

- **T1:** safe constrained communication fallback may occur when permitted;
- **T2:** invalid/failed interpretation cannot mutate durable evidence;
- **T3:** invalid/failed planning/generation cannot activate/lock authoritative artifacts;
- **T4:** invalid/failed judgment cannot finalize official marks/outcomes.

T0 remains deterministic and promptless.

### 17.10 Criticality

D03 must represent C1–C4 prompt criticality separately from T0–T4 authority.

Authority answers “what consequence may this capability influence?”

Criticality answers “how rigorously must this family/capability be evaluated/operated?”

Neither dimension may silently overwrite the other.

### 17.11 Route manifest

Create/version the Teaching AI route manifest outside prompt text.

Each family/capability route definition should include:

- candidate runtime class/posture;
- reasoning intent;
- quality floor;
- allowed primary route(s);
- allowed fallback route(s);
- degradation permission;
- criticality/risk requirements;
- preparation-stage applicability where relevant;
- qualification status;
- qualification evidence reference placeholders;
- route version.

D03 route qualification state must remain **UNQUALIFIED**.

No route is permitted to inherit qualification from:

- a different family;
- a different capability;
- a different stage;
- its primary route;
- a previous model/version/settings combination.

### 17.12 Preparation Profiles / PPL route postures

Implement versioned Preparation Profile configuration covering, at minimum:

- eligible target kinds;
- consequence/criticality;
- lead-time policy;
- expected volatility;
- review cadence/budget;
- candidate budget;
- required independent-review stages;
- freeze/finalization rules;
- permitted preparation stages;
- route-posture requirements.

Support the canonical preparation route postures:

- `economy_maintenance`
- `bounded_interpretive`
- `strong_design`
- `independent_validation`
- `final_reconciliation`

Teaching code and prompt text must not hard-code provider/model IDs.

Cheap/economy routes cannot cross gates that require stronger independently qualified review.

A strong/final pass must be allowed to overturn earlier cheaper-route proposals using primary authoritative evidence.

### 17.13 PPL request-contract extension

Extend the capability/request contract to carry structured preparation metadata such as:

- workspace ref/version;
- preparation stage;
- maturity;
- previous artifact ref/version;
- authoritative input bundle/version;
- material delta;
- finding refs;
- review purpose;
- maturity target;
- protection class;
- route posture;
- idempotency key;
- correlation/causation metadata.

This contract work must not implement D05 orchestration behavior prematurely.

### 17.14 Telemetry/version lineage

Extend D02 safe telemetry to record, where applicable:

- execution/correlation ID;
- capability ID;
- family/version;
- Constitution version;
- schema/contract version;
- route-manifest version;
- candidate route identifier/class;
- qualification status at execution time;
- validation outcome;
- deterministic rejection reason;
- downstream owner/mutation ref where one later exists.

Do not persist:

- hidden chain-of-thought;
- raw prompt bodies unless separately approved for controlled diagnostics;
- full student responses merely for telemetry;
- assessment answers/source passages unnecessarily;
- secrets/tokens.

---

## 18. D03 strict must-not boundary

D03 must **not**:

- rewrite frozen prompt-family prose merely to make tests pass;
- silently adopt an unapproved newer prompt pack;
- create a hybrid prompt manifest;
- assign a prompt family to any T0 capability;
- reduce 22 T0 back to the historical 18;
- treat historical 165 capability counts as current acceptance truth;
- allow prompt-family membership to raise authority;
- hard-code Gemini/OpenAI/provider/model IDs into Teaching domain/runtime contracts;
- call a provider SDK directly from Teaching feature/domain code;
- bypass the central KIWI AI Orchestrator;
- mark any Teaching route QUALIFIED/PRODUCTION_QUALIFIED;
- let fallback routes inherit qualification;
- let cheap routes cross stronger-required finalization gates;
- let untrusted course/student/source content enter trusted instruction lanes;
- store hidden chain-of-thought;
- let raw/schema-only model output mutate academic truth;
- create Gradebook/SKM/Progression/Attendance/Course/Assessment/Request/Teacher-Identity/Scheduler truth;
- implement D04 academic persistence;
- implement the D05 Teaching Orchestrator service;
- reintroduce generic Teaching availability toggles removed by the accepted Class-E amendment;
- fix unrelated platform RLS/security debt without owning change control;
- weaken D02 fail-closed/fairness behavior to improve apparent availability;
- treat “specification complete” as “empirically production qualified.”

---

## 19. D03 mandatory verification and acceptance checklist

D03 is not complete merely because registry/configuration files exist.

Before D03 may be marked ACCEPTED, the implementation agent must provide evidence for all of the following:

### Scope/accounting

- all 36 D03 TCH IDs accounted for;
- none silently deferred;
- none completed by redefining the task;
- no D04/D05 task pulled forward materially.

### Registry integrity

- exactly 169 canonical capabilities resolve;
- exactly 147 model-eligible capabilities resolve;
- exactly 22 T0/no-direct-prompt capabilities resolve;
- all four PPL T0 capabilities are present and promptless;
- no duplicate canonical IDs;
- no unknown IDs;
- stable alias/deprecation behavior tested.

### Prompt-family integrity

- exactly 19 canonical families registered;
- each model-eligible capability resolves to exactly one primary family unless the canonical contract explicitly defines a non-primary/support relation;
- every T0 resolves to no direct prompt family;
- manifest versions/hashes match the accepted canonical prompt baseline;
- superseded/unmanifested prompt text is rejected;
- family/task mode does not change capability authority/owner.

### Constitution/contract integrity

- every model-backed invocation binds a Constitution version;
- precedence tests prove lower-level instructions cannot override higher-level rules;
- Teacher Personality cannot alter academic standards/authority;
- Student Intake/source/uploads/responses cannot become trusted instructions;
- T4 context allowlists/minimization are enforced;
- insufficient-evidence/review-needed states are supported.

### Route-control integrity

- all Teaching AI routes remain UNQUALIFIED;
- production activation remains blocked;
- primary/fallback route definitions are versioned;
- fallback does not inherit qualification;
- preparation route postures exist;
- cheap routes cannot cross stronger-required gates;
- no provider/model names are hard-coded in Teaching academic/runtime contracts.

### D02 regression

- D02 runtime tests still pass;
- D01 verifier still passes;
- full Teaching tests pass;
- full KIWI regression suite passes;
- web build passes;
- syntax/static checks pass;
- no direct provider SDK imports appear in Teaching domain/feature code;
- D02 trusted-validation and commit-gateway boundaries remain intact.

### Persistence/security

- if D03 legitimately adds operational persistence, migration history/rollback/security must be documented;
- if D03 adds no persistence, the handoff must explicitly say **NONE**;
- no D04 academic tables/schema may be introduced;
- no browser-authoritative academic write path may be created.

### Deployment

- GitHub implementation PR merged;
- accepted merge SHA recorded;
- CI on the accepted merge is green;
- Vercel deployment state for the accepted merge recorded;
- Render backend/logs re-verified if D03 changes live backend/runtime behavior;
- Supabase migration/schema state recorded if changed.

### Defects

- no unresolved D03-attributable P0/P1;
- every skip/known limitation documented with severity and ownership;
- every architecture exception/change-control record explicitly listed;
- source-integrity decisions recorded, especially prompt-manifest authority.

Only after all applicable checks above pass may D04 be marked UNLOCKED.

---

## 20. Mandatory D03 → D04 handoff requirements

When D03 is fully implemented, tested, merged and accepted, create:

- repository handoff: `docs/teaching/handoffs/D03.md`
- user-downloadable handoff: `KIWI_Teaching_D03_to_D04_Handoff.md`

Then **STOP**.

Do not begin D04 in the same delivery.

The D03 → D04 handoff must be at least as operationally complete as this handoff and the accepted D00 → D01 handoff. It must include:

- delivery title/state/date;
- canonical roadmap/version;
- exact D03 start SHA;
- branch/PR;
- pre-merge tested head;
- accepted merge SHA;
- exact planned/completed/deferred TCH IDs;
- task-by-task completion summary for every D03 TCH ID;
- canonical sources materially relied upon;
- source-integrity issues and how each was resolved;
- exact accepted capability counts;
- exact prompt manifest/family versions and hashes used;
- exact Constitution version used;
- module/folder/file changes;
- public interfaces/contracts added or changed;
- runtime/authority/context/validation/route invariants;
- route-manifest state and proof all routes remain UNQUALIFIED;
- PPL Preparation Profile/route-posture implementation state;
- persistence/migrations or explicitly NONE;
- RLS/grants/security changes or explicitly NONE;
- telemetry/observability changes;
- tests actually run with exact pass/fail/skip counts;
- CI workflow/check names and accepted-merge result;
- Vercel verification;
- Render verification;
- Supabase verification;
- security/privacy/academic-authority review;
- known defects/debt;
- architecture exceptions/change-control records;
- rollback/recovery notes;
- exact D04 prerequisites now satisfied;
- exact items D04 must preserve;
- exact things D04 must not implement prematurely;
- minimum accepted implementation baseline;
- current-live-main rule;
- explicit D04 unlocked/blocked decision;
- explicit instruction to stop after producing the handoff.

The handoff must distinguish:

- facts actually verified;
- facts inherited from earlier accepted handoffs;
- things not re-verified;
- expected/intentional skips;
- unresolved blockers.

It must never claim deployment, migration, CI or runtime verification that was not actually observed.

---

## 21. Canonical AI handoff reproduction template

The following template is now the minimum standard for future KIWI Teaching delivery handoffs.

An implementation AI completing D03 or any later delivery must reproduce this structure, adapting it to the delivery rather than shortening it into a summary.

```md
# KIWI Teaching — DXX → DYY Implementation Handoff

**Delivery:** DXX — <canonical title>
**State:** ACCEPTED | BLOCKED | PARTIAL
**Date:** YYYY-MM-DD
**Canonical roadmap:** <version>
**Start baseline:** <sha>
**Implementation branch:** <branch>
**Implementation PR:** #<n>
**Pre-merge tested head:** <sha>
**Accepted implementation merge:** <sha or NONE>

## 1. Completed scope
- exact planned TCH IDs
- exact completed TCH IDs
- exact deferred/blocked TCH IDs
- task-by-task outcome for every assigned TCH ID
- explicit statement that no next-delivery scope was pulled forward

## 2. Canonical sources materially relied upon
- exact canonical filenames/versions
- accepted predecessor handoff
- live repository/runtime sources
- source-integrity conflicts
- change-control resolution
- exact hashes where canonically relevant

## 3. Starting-state/preflight record
- live main SHA
- relevant modules/contracts discovered
- migration head/schema state
- deployment state
- environment/config state
- pre-existing defects/debt
- unresolved architecture exceptions

## 4. Implementation changes
Describe each workstream separately:
- files/modules added/changed
- behavior implemented
- boundaries preserved
- what was deliberately not implemented

## 5. Interfaces/contracts/invariants
- public/internal interfaces added
- compatibility rules
- authority ownership
- state/version rules
- context/security rules
- failure/fairness rules
- non-negotiable carry-forward invariants

## 6. Persistence/migrations
- migrations added/applied
- exact migration IDs
- schema/table changes
- RLS/grants/policies
- production data migration
- migration-history repair if any
- rollback/recovery posture
OR explicitly: NONE

## 7. Security/privacy/academic-authority review
- browser/server privilege boundaries
- prompt/content injection boundaries
- secrets
- context minimization
- protected/high-stakes data
- authoritative owner checks
- KIWI-failure fairness

## 8. Observability/operations
- telemetry fields
- correlation/version lineage
- logs/metrics
- excluded sensitive/CoT data
- runtime tunables

## 9. Tests actually executed
For every command/check:
- exact command
- total/pass/fail/skip
- expected skips and why
- production-refusal guards
- accepted-merge CI result
Never replace actual counts with “tests passed.”

## 10. Deployment verification
### GitHub
- PR
- accepted SHA
- CI checks
### Supabase
- project
- migration/schema verification
- advisors/security findings
### Vercel
- deployment ID
- Git SHA
- target/state
### Render
- service/deployment/log verification
- or explicitly NOT RE-VERIFIED and why

## 11. Known defects/debt
For each:
- issue
- severity
- ownership
- impact
- whether it blocks next delivery

## 12. Architecture exceptions/change control
- every exception or NONE
- approved change-control artifacts
- canonical baseline amendments

## 13. Next-delivery assignment
- exact canonical title
- hard predecessor(s)
- exact TCH scope
- objective
- acceptance gate
- must-not boundary

## 14. Mandatory next-delivery preflight
- canonical docs to read
- repository/runtime surfaces to inspect
- infrastructure to verify
- conflicts that must be resolved before coding

## 15. Carry-forward architecture
- interfaces to reuse
- sources of truth
- authority ceilings
- failure behavior
- context/security boundaries
- route/prompt/runtime qualification state

## 16. Next-delivery acceptance checklist
- exact evidence required before acceptance
- exact tests/deployment/security evidence
- explicit P0/P1 closure requirement

## 17. Final acceptance record
- Delivery
- State
- Start SHA
- Accepted merge SHA
- CI
- tests
- migrations
- deployments
- exceptions
- next delivery UNLOCKED/BLOCKED

## 18. Mandatory next handoff
- exact repository filename
- exact downloadable filename
- required content standard
- STOP instruction

After creating this accepted handoff, STOP.
Do not begin the next delivery in the same implementation session unless the user explicitly starts that delivery.
```

### Handoff-generation instruction to future AI

When generating a KIWI Teaching delivery handoff:

- do not summarize away operational evidence;
- do not infer completion from conversation;
- inspect the repository/runtime state first;
- cite exact SHAs, migration IDs, test counts and deployment states;
- explicitly say when something was not re-verified;
- carry forward unresolved debt without silently “fixing” it;
- distinguish historical facts from current accepted facts;
- resolve canonical conflicts through frozen artifacts/change control;
- never invent missing hashes, deployment results, test results or migration state;
- record exact next-delivery scope and stop boundary;
- make the handoff sufficient for a fresh AI with no conversational memory to continue safely.

The purpose of the handoff is not merely documentation. It is the durable execution state between disposable implementation chats.

---

## 22. Final D02 → D03 execution instruction

D02 remains ACCEPTED.

D03 is UNLOCKED subject to its mandatory preflight and prompt-source authority resolution.

The D03 implementation agent must start from **current live `main`**, not from conversational memory and not from the historical minimum implementation SHA alone.

The minimum accepted D02 implementation baseline remains:

`eba01147b4be1bb5293ddc15b6d6f4cc8282c469`

But all later accepted documentation/corrections on `main` must also be included.

D03 must complete its exact 36-task scope, preserve all D02 authority/runtime/security invariants, keep all Teaching AI routes UNQUALIFIED, produce an exhaustive D03 → D04 handoff using Section 21, and then stop.
