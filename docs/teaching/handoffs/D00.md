# KIWI Teaching — Delivery Handoff

**Delivery:** D00 — Canonical Baseline & Delivery Governance  
**State:** ACCEPTED  
**Date:** 2026-09-25  
**Canonical roadmap version:** Phase 19 v1.4 FINAL  
**Start commit:** `3641391d4c9077ad0740cf6e5693b2644d31a0df`  
**End commit:** `4da97a6cb22598538c7ad29ffe7e1118ca826873`

## 1. Scope

**Planned TCH IDs:** TCH-0001–TCH-0015, TCH-0670–TCH-0674, TCH-0744, TCH-0749–TCH-0752, TCH-0871

**Completed TCH IDs:** TCH-0001–TCH-0015, TCH-0670–TCH-0674, TCH-0744, TCH-0749–TCH-0752, TCH-0871

**Deferred/not completed:** NONE within D00. D01 remains responsible for repository/module/test foundation and feature flags; D30 remains responsible for empirical Teaching-AI/PPL route qualification; D31 remains responsible for production release authorization.

D00 intentionally implemented governance/foundation only. It did not implement Teaching feature behavior, Teaching persistence, provider/model bindings, prompt changes, or future-delivery runtime logic.

## 2. Canonical sources actually used

Primary implementation baseline:

- `KIWI_Teaching_Implementation_Context_Pack_v1.7.md`
- `KIWI_Teaching_D00_Launch_Packet_v1.3.md`
- `KIWI_Teaching_Phase22_Final_Implementation_Freeze_v1.3.md`
- `KIWI_Teaching_System_Blueprint-11.5.md`
- `KIWI_Teaching_Master_Implementation_Backlog-9.5.md`
- `KIWI_Teaching_Phase19_Delivery_Roadmap_Overlay_v1.4_FINAL.md`
- `KIWI_Teaching_Delivery_Task_Map_v1.5.json`
- `KIWI_Teaching_Capability_Registry_v1.1.md`
- `KIWI_Teaching_Orchestrator_Event_Runtime_Spec_Phases_7-8_v1.1.md`
- `KIWI_Teaching_Progressive_Preparation_Lifecycle_Standard_v1.0.md`
- `KIWI_Teaching_Phase15_Final_Prompt_Manifest_v1.2.json`
- `KIWI_Teaching_Phase15_All_19_Prompt_Families_FINAL_DESIGN_FROZEN_v1.2.md`
- `KIWI_Teaching_Capability_Traceability_Matrix_v1.1.json`
- `KIWI_Teaching_Critical_Invariant_Trace_Map_v1.1.md`
- `KIWI_Teaching_Implementation_Change_Control_Protocol_v1.0.md`
- `KIWI_Teaching_Implementation_Delivery_Handoff_Template_v1.0.md`

The freeze also pins Phase-16 Evaluation v1.4, PPL Empirical Qualification v1.0, Original-18-T0 PPL Review v1.0, and the current Authority Spec v1.1. Their D00-relevant release holds/counts were resolved through the hash-valid freeze/roadmap/registry/traceability sources.

Source-integrity note: the locally retained file bearing the Authority Spec name did not match the frozen v1.1 SHA-256 and was rejected as current authority. This is recorded in `docs/teaching/governance/source-resolution.md`.

Live implementation sources inspected:

- GitHub `main` at D00 start.
- Existing `index.js`, `index.html`, `public/teaching-frontend.js`, `teaching-backend.js`.
- Existing KIWI AI Orchestrator modules/tests.
- Current Supabase project/schema/migration history.
- Current Vercel project/deployment.
- Render workspace discovery; service-level inspection was not performed because the Render connector requires explicit workspace selection confirmation.

## 3. Implementation changes

Added durable governance implementation under `docs/teaching/governance/`:

- `canonical-baseline.json` — pins canonical versions/hashes, 902-task/32-delivery baseline, 169/147/22 capability counts, 19 prompt families, D00 task set, PPL T0 capabilities, route postures, D30/D31 holds.
- `README.md` — precedence and implementation-governance boundary.
- `source-resolution.md` — D00 start record, live infrastructure state, canonical retrieval/integrity notes and pre-existing implementation observations.
- `decision-log.md` — durable decisions/exceptions, including PPL adoption and separate Teaching application-surface realization boundary.
- `terminology.md` — canonical vocabulary and enum/display-label separation.
- `authority-and-state-contracts.md` — source-of-truth owners, Gradebook/SKM/Progression truth separation, Course/Class/Assignment state-axis separation, fairness/personality/explainability rules.
- `intelligence-and-ppl.md` — responsibility classes, T0–T4 authority ceilings, central AI routing, context minimization, PPL boundaries and four deterministic PPL capabilities.
- `release-scope.md` — internal Alpha, basic Teaching Beta and separate assessment/grading Beta scope.
- `dependency-change-control.md` — canonical dependency metadata convention and contradiction/change-control process.
- `blueprint-traceability.md` — D00–D31 epic-to-Blueprint traceability checklist.

Added `scripts/verify-teaching-d00-governance.js` to fail closed on core baseline-count/hash/task/PPL/release-hold drift.

Added `.github/workflows/teaching-d00-governance.yml` to run the D00 verifier and the existing KIWI regression suite on relevant governance changes.

No Teaching feature behavior was added by D00.

## 4. Persistence and migrations

**Migrations:** NONE.

**Schema/RLS/policy changes:** NONE.

Supabase start environment:

- project `nqdwifqskxkblgdgeutn` / Kiwi;
- PostgreSQL 17;
- migration head `20260924000506_reckoning_resumable_preparation_delivery_d`;
- no Teaching-specific persistence existed at D00 start.

D04 owns Teaching persistence. D00 deliberately did not create an early/parallel source of academic truth.

Pre-existing Supabase security debt observed, not modified by D00: RLS is disabled on `public.background_jobs`, `public.biome_zones`, `public.biome_zone_descriptions`, `public.onboarding_state`, and `public.notifications`. Enabling RLS without correct policies could break current access, so D00 did not patch these tables.

Production data migration required for D00: **NO**.

Rollback: revert D00 governance commits; no database rollback is required.

## 5. Interfaces and invariants

D00 introduces governance contracts, not runtime academic APIs.

Interfaces/contracts now pinned for later implementation:

- permanent TCH identity + explicit delivery/dependency/blocker metadata;
- one authoritative owner per academic fact;
- Gradebook truth, Knowledge Model truth and Progression truth remain distinct;
- Course lifecycle / overlays / progression outcome remain separate axes;
- Class lifecycle / instructional substate / attendance outcome remain separate axes;
- Assignment lifecycle / orthogonal conditions remain separate axes;
- internal enums do not automatically become student-facing labels;
- KIWI-caused failure cannot directly create academic penalty;
- Teacher Personality cannot alter academic truth/rules;
- formal academic actions must be human-explainable;
- hidden confidence/weights are not false-certainty student metrics;
- responsibility classes do not define model-call topology;
- every model-backed capability later declares a maximum T0–T4 authority ceiling;
- canonical capability identity comes from the Capability Registry, not INV aliases;
- capability-scoped context minimization is mandatory;
- PPL is event-driven/versioned/bounded and is not an academic owner or continuous autonomous agent;
- four PPL deterministic T0 capabilities remain promptless;
- provider/model selection remains central AI Orchestrator configuration;
- Teaching AI/PPL routes remain unqualified until D30;
- production release remains unauthorized until D31.

The separate Teaching application surface is a UI/deployment boundary only. It must not create duplicate Gradebook, SKM, Progression, Attendance, Course-scope, Assessment-Attempt, Request, Scheduler or other academic-truth ownership.

## 6. Tests actually executed

GitHub Actions workflow: **Teaching D00 Governance**, run **36115609469**, job **108008870960**.

Executed on Node 22:

1. `node scripts/verify-teaching-d00-governance.js`
   - Result: PASS
   - Evidence: `[D00] PASS: canonical governance baseline is internally consistent.`

2. `npm test` → `node --test tests/ai/*.test.js`
   - Tests: **352**
   - Passed: **352**
   - Failed: **0**
   - Cancelled: **0**
   - Skipped: **0**
   - Todo: **0**

Vercel status for the D00 branch head before this handoff commit: success.

No D00 database migration test was required because D00 made no persistence change.

## 7. Security / privacy / academic-authority review

Passed D00 governance checks for:

- explicit authoritative ownership;
- no AI output promoted directly to academic truth;
- T0–T4 authority ceilings pinned;
- provider/model bindings excluded from Teaching domain/prompt contracts;
- context-minimization rule explicitly prohibits irrelevant attendance, reputation, Teacher Personality, prior GPA, unrelated marks or behavioral history from formal marking unless approved academic policy requires them;
- PPL protected-content isolation is recognized as a security/context boundary, not a prompt-only safeguard;
- KIWI-caused failure fairness is explicit;
- no hidden chain-of-thought persistence architecture is authorized.

Pre-existing security issue carried forward: `index.js` contains a hard-coded fallback database credential. D00 did not edit unrelated runtime code. D01 security/secret-audit work must remove the source credential and rotate it if still valid.

## 8. Known defects and debt

1. **Standalone Teaching frontend boundary — current D01 starting point.**  
   After D00 acceptance, the pre-canonical in-app overlay was removed and replaced by `public/teaching.html` + `public/teaching.js` in commit `19892c1037739222d6fe5a3baade662a59b1a545`. D01 must treat those two files as the Teaching frontend entry points. `index.html` remains the main KIWI app document and `index.js` remains the main KIWI backend/core entry. Teaching must reuse KIWI through stable shared interfaces/services rather than importing the normal KIWI shell or duplicating its academic truth.

2. **No canonical Teaching feature flag yet — expected D01 work.**  
   The prototype is currently unflagged. TCH-0029/TCH-0675-era feature/config boundaries belong to D01/later mapped scope.

3. **Hard-coded database fallback credential — pre-existing security debt.**  
   Must be removed from source and rotated if valid under D01's secret audit. Not introduced by D00.

4. **Supabase RLS-disabled existing public tables — pre-existing security debt.**  
   Must be remediated with correct policies by the owning security delivery; not blindly patched in D00.

5. **Canonical source availability/integrity debt.**  
   A retained Authority-Spec file failed frozen-hash verification; exact current body must be resolved before any later delivery depends on details unique to that artifact.

No known P0/P1 defect attributable to D00 remains unresolved.

## 9. Architecture exceptions

No frozen academic architecture was silently amended.

Post-D00 realization note: the pre-canonical in-monolith Teaching overlay was removed before D01 start. The current frontend boundary is a standalone `public/teaching.html` document driven by `public/teaching.js`, while KIWI's `index.html` and `index.js` remain the main application/core entry points. This realization must not be mistaken for completion of D01's module/service/repository contracts.

Recorded source-integrity exception: a retained Authority-Spec copy does not match the frozen v1.1 hash and is excluded from authority.

## 10. Deployment/runtime impact

- No environment variable added/changed.
- No Supabase migration/schema change.
- No Render service/config mutation.
- No provider/model route activation.
- No prompt/version change.
- No production Teaching feature activation.
- Vercel continues using the existing deployment behavior; branch deployment/status was successful.
- PR #80 merged to `main` as `4da97a6cb22598538c7ad29ffe7e1118ca826873`.
- Post-D00 standalone-shell correction merged as `19892c1037739222d6fe5a3baade662a59b1a545`; it changed no deployment configuration.
- D00 adds documentation, a verifier script and a governance CI workflow only.

Rollback trigger: unexpected CI/runtime side effect from governance workflow/files. Rollback is code-only because D00 has no persistence mutation.

## 11. Next-delivery readiness

**Next delivery:** D01 — Repository, Module & Test Foundation

**Prerequisites satisfied:** YES. PR #80 merged successfully as `4da97a6cb22598538c7ad29ffe7e1118ca826873`. Evidence: all 26 D00 TCH items are represented in durable governance artifacts; D00 verifier passes; existing regression suite passes 352/352; no D00 migrations or feature behavior were introduced.

**Required carry-forward context:**

- Canonical baseline: Blueprint v11.5 / Backlog v9.5 / Roadmap v1.4 / Task Map v1.5.
- Counts: 902 tasks, 32 deliveries, 169 capabilities = 147 model-eligible + 22 T0, 19 prompt families.
- Four PPL T0 capability IDs in `canonical-baseline.json`.
- Teaching AI/PPL route qualification remains blocked until D30; production remains blocked until D31.
- `public/teaching.html` and `public/teaching.js` are the Teaching frontend entry points and must remain visually/shell-separated from the normal KIWI navigation.
- `index.html` remains KIWI's main app document; `index.js` remains the main backend/core entry. D01 should extract/reuse stable shared interfaces from KIWI rather than duplicate logic into Teaching.
- Teaching must share authoritative KIWI/Teaching domain truth rather than fork it.
- `teaching-backend.js` is only the current baseline Teaching backend seam; D01 must build the proper service/repository/config/test boundaries around the existing KIWI core.
- Remove/rotate hard-coded DB fallback credential in D01 security audit.
- Resolve exact canonical Authority Spec body before implementing any rule uniquely dependent on it.

**Recommended D01 starting baseline:** `19892c1037739222d6fe5a3baade662a59b1a545`.

## 12. Mandatory D01 completion handoff

When D01 is fully implemented, tested, merged and accepted, the D01 implementation must produce the canonical next-delivery handoff **before stopping**.

Required repository artifact:

`docs/teaching/handoffs/D01.md`

Required purpose:

- hand D01's accepted implementation state to D02;
- list every completed D01 TCH ID;
- record code/module/interface/config/security changes;
- record migrations (or explicitly NONE);
- record tests and exact results;
- record commit SHA(s);
- record known limitations/deferred items/architecture exceptions;
- record canonical files materially relied upon;
- state the exact accepted baseline from which D02 should start;
- explicitly direct D02 to the current Teaching/KIWI entry boundaries where relevant.

D01 must **not begin D02** after producing the handoff.
